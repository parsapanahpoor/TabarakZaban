using System.Reflection;

namespace Identity.Persistence;

public static class IdentityPersistenceAssemblyReference
{
    public static readonly Assembly Assembly = typeof(IdentityPersistenceAssemblyReference).Assembly;
}